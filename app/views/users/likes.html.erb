<h1><%= @user.name %>のお気に入り一覧</h1>

<ul>
    <% @tasks.each do |task| %>
        <li><%= task.user.name %> : <%= link_to task.id, task %> <%= task.content %> : <%= task.status %></li>
    <% end %>
</ul>

<%= link_to "タスク一覧へ戻る", tasks_path %>